<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Recuperar Contraseña</title>
    <link rel="stylesheet" href="{{ asset('css/recuperar_contraseña.css') }}">
</head>
<body>
    <div class="container">
        <h1>Recuperar Contraseña</h1>

        {% if error %}
            <p class="error">{{ error }}</p>
        {% endif %}

        {% if success %}
            <p class="success">{{ success }}</p>
            <p>Revisa tu correo y usa el código de recuperación.</p>
        {% else %}
            <form action="{{ path('procesar_recuperacion') }}" method="POST">
                <div class="form-group">
                    <label for="email">Introduce tu correo electrónico:</label>
                    <input type="text" id="email" name="email" required>
                </div>
                <button type="submit">Enviar Código</button>
            </form>
        {% endif %}

        <div class="links">
            <p><a href="{{ path('ctrl_login') }}">Volver al Login</a></p>
        </div>
    </div>
</body>
</html>

