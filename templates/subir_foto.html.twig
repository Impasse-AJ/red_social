{% extends 'base.html.twig' %}

{% block title %}Subir Foto de Perfil{% endblock %}

{% block styles %}

{% endblock %}

{% block body %}
    <div class="container">
        <h1>Subir Foto de Perfil</h1>
<link rel="stylesheet" href="{{ asset('css/subir_foto.css') }}">
        {% if error is defined and error %}
            <p class="error">{{ error }}</p>
        {% endif %}
        <form method="POST" enctype="multipart/form-data">
            <input type="hidden" name="_token" value="{{ csrf_token('subir_foto') }}">
            <input type="file" name="foto" accept="image/*" required>
            <button type="submit">Subir</button>
        </form>

        <a href="{{ path('ver_perfil', { id: usuario.id }) }}">Volver al Perfil</a>
    </div>
{% endblock %}
